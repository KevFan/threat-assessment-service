apiVersion: v1
kind: Service
metadata:
  name: threat-assessment-service
  namespace: security
  labels:
    app: threat-assessment-service
spec:
  type: ClusterIP
  selector:
    app: threat-assessment-service
  ports:
    - name: grpc
      port: 8080
      targetPort: grpc
      protocol: TCP
